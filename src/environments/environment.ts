// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  cv_key: "9339a37da7c94b028eac0841658a36ce",  // top page
  cv_endpoint: "https://jpyutanitkhack-prediction.cognitiveservices.azure.com/",  // top page
  project_id: "5dc2c406-eecb-43c1-b926-fac6fee7fe15",  // project page
  published_name: "model",  // when publishing time
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
